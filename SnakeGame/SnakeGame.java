package SnakeGame;

public class SnakeGame {
    public static void main(String[] args) {
//        GameFrame frame = new GameFrame(); -> if we don't plan to use frame further we can just create object without instance also
        new GameFrame();

    }
}
